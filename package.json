{
  "name": "backend-expressjs",
  "module": "src/server/index.ts",
  "type": "module",
  "scripts": {
    "migrate:dev": "bun prisma migrate dev",
    "migrate:deploy": "bun prisma migrate deploy",
    "migrate:status": "bun prisma migrate status",
    "generate": "bun prisma generate",
    "dev": "bun run --watch src/server/index.ts",
    "start": "bun run src/server/index.ts"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "prisma": "^6.0.1"
  },
  "peerDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@prisma/client": "^6.0.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0"
  }
}
